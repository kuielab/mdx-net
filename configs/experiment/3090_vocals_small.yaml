# @package _global_

# to execute this experiment run:
# python run.py experiment=example_simple.yaml

defaults:
  - 3090_vocals.yaml

datamodule:
  target_name: vocals

model:
  num_blocks: 3
  l: 1
  g: 1
  k: 1
  dim_f: ${dim_f}
  dim_t: ${dim_t}
  n_fft: ${n_fft}
  hop_length: ${hop_length}
  bn:
  bias: # deprecated